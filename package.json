{
  "name": "Simple_pomodoro",
  "version": "1.0.0",
  "description": "Just a simple an configurable pomodoro timer",
  "main": "app.js",
  "scripts": {
    "start": "nodemon --exec electron ./app.js",
    "electron": "npx electron ./app.js",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "main": "build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "build": {
    "appId": "com.fuijiin",
    "productName": "Simple pomodoro",
    "directories": {
      "output": "distOneClick"
    },
    "icon":"./assets/tomato.png"
    
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^19.0.8",
    "electron-builder": "^23.1.0",
    "nodemon": "^2.0.19"
  }
}
